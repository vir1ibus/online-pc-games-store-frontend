export default function NotFound() {
    return (
        <main id="content" className="text-center">
            <span className="fs-1 text-warning">Страница не найдена</span>
        </main>
    )
}